function usersReducer(state = { currentUser: {user_id: 1, name: "Isabelle", email: "imletaconnoux@gmail.com"}}, action){

  switch (action.type) {
    default:
        return state
    }
}

export default usersReducer
